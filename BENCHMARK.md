# BENCHMARK

This file is produced by the `Main CI + Release` workflow on every merge/push to `main`.

It contains:
- full mint-check pass/fail status for all sample mints
- run durations
- release-attached benchmark output for traceability

The latest generated benchmark is also attached to each GitHub release.
